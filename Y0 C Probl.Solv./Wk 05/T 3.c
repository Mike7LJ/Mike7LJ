/* Print a square of asteriks on the screen​ 

Ypur printf only one * per line
Use nested loops */

#include <stdio.h>

int main()
{
	for (int i=0,i<3,i++){ //Loop to print the asterisks
		for("* ");
		i++;
	}
	printf("\n"); //Move to the next line
	return 0;
}