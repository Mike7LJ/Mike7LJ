#include <stdio.h>
#include <string.h>

void main(){
    char str[20];
    char v[]='aeiou';';

    printf("Enter The String: ");
    fgets(str,sizeof(str), stdin);
    str[strlen(str)-1] = '\0';
    int n=strlen(str);

    for(int i=o;i<n/2;)
    
        printf("The String:%s", str);
       
        
    }